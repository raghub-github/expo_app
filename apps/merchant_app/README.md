# Merchant App (placeholder)

This folder will host the Merchant app later (mobile/web).

Constraint: it must reuse:
- `packages/contracts` (shared DTOs/models)
- `packages/sdk` (typed API client)
- `backend` (same API + database)


