/** Short keys matching GET /v1/app-assets/merchant → assets map. */
export const MX = {
  auth: {
    header: "auth.header",
    welcome: "auth.welcome",
    logo: "auth.logo",
  },
  map: {
    bike: "map.bike",
  },
} as const;
