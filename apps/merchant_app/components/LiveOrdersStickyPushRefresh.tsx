/**
 * Legacy hook for order-lifecycle push → sticky tray sync.
 * Sticky tray is disabled; this component is a no-op kept for stable imports.
 */

export default function LiveOrdersStickyPushRefresh() {
  return null;
}
