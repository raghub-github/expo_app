/**
 * Shim — Restaurant status moved to `/restaurant-status` (outside tabs).
 * Kept so Metro/hot-reload does not break on stale imports of this path.
 */
export { default } from "@/components/restaurant-status/RestaurantStatusScreen";
