export {
  NAV_CAMERA_MIN_INTERVAL_MS,
  NAV_CAMERA_MIN_MOVE_M,
  NAV_CAMERA_MIN_BEARING_DELTA,
  NAV_CAMERA_STATIONARY_SPEED_MPS,
  shouldThrottleNavigationCamera,
  normalizeBearing,
  shouldSkipStationaryCamera,
} from "@gatimitra/map-tracking-engine";
