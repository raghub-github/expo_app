/** Hide road route when rider has essentially reached the destination pin. */
export {
  NAV_ROUTE_HIDE_DISTANCE_M,
  shouldHideNavigationRoute,
  resolveRoadRouteCoordinates,
} from "@gatimitra/map-tracking-engine";
