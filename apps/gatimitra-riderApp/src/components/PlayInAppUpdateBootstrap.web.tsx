/**
 * Web stub — `sp-react-native-in-app-updates` only ships `.android` / `.ios`
 * entry points, so importing it into the Expo web/SSR graph fails Metro.
 */
export function PlayInAppUpdateBootstrap() {
  return null;
}
