## Rider app config (example)
## Copy to `apps/gatimitra-riderApp/.env` for local dev.
##
## Expo rule: only EXPO_PUBLIC_* are available in the app bundle.
## Do NOT put secrets here.

# Backend base URL
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# OTP is backend-driven (MSG91 via backend). Rider app must not integrate OTP providers directly.
EXPO_PUBLIC_OTP_PROVIDER=msg91

# Mapbox Public Token (REQUIRED for maps to work)
# Get your token from: https://account.mapbox.com/
# Token should start with "pk."
EXPO_PUBLIC_MAPBOX_PUBLIC_TOKEN=pk.eyJ1Ijo...

# Mapbox Download Token (REQUIRED for EAS builds)
# Used during native build to download Mapbox SDK
# Can be same as public token, or use a secret token
RNMAPBOX__MAPS_DOWNLOAD_TOKEN=pk.eyJ1Ijo...

## Map/Location notes:
## - This app uses Mapbox SDK for maps (requires EAS build, not Expo Go)
## - Location tracking uses `expo-location`
## - For accurate tracking you should test on a real device with GPS enabled
## - See EAS_BUILD_GUIDE.md for build instructions


