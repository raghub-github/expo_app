# Ride card images

Add optimized images here for ride service cards (recommended size: 64×64 or 128×128 px):

- `bike.png` – Bike Ride
- `auto.png` – Auto
- `cab.png` – Cab

Use these filenames so the app can load them from the public folder. Do not use hardcoded external URLs.
