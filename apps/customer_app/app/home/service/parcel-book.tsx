/**
 * Parcel book (inner) — /home/service/parcel-book
 */

import { ParcelBookScreen } from "@/features/parcel/ParcelBookScreen";

export default function ParcelBookRoute() {
  return <ParcelBookScreen />;
}
