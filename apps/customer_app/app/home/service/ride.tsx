/**
 * GatiMitra Ride Booking – route for /home/service/ride.
 * Ride-only screen; no parcel or courier.
 */

import { RideBookingScreen } from "@/features/ride/RideBookingScreen";

export default function RideServiceScreen() {
  return <RideBookingScreen />;
}
