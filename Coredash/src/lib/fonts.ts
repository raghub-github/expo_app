import { Poppins } from "next/font/google";

/** Same face Control Dashboard uses on sidebar nav buttons. */
export const poppinsUi = Poppins({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
  display: "swap",
});
