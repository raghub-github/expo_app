-- Rollback: 0464_gmmc1026_fssai_reject_reset_for_retest
-- Note: Prior FSSAI rejection reason / step-4 rejection row are not restored
-- (they were intentionally cleared for a clean re-test). No-op rollback.

SELECT 1;
